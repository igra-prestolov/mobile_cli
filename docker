Скачать образ
docker pull

Список образов
docker images

Список контейнеров
docker ps -a

Удалить контейнеры
docker rm -f <имена или айди контейнеров>
docker rm $(docker ps -a -q -f status=exited)

Удалить образ
docker rmi <имя или айди образа>

Запуск контейнера
docker run <имя контейнера>
-d - открепить от терминала
-P - прокинуть все порты
—-name - задать имя контейнера
--net - задать имя сети
--rm - удалять контейнер по завершении

Запуск нескольких команд в контейнере
docker run -it <имя образа> sh

Выключить контейнер
docker stop <имя или айди контейнера>

Создать образ
docker build -t <имя пользователя>/<имя проекта> .
-t - опциональный тэг

Показать все существующие сети
docker network ls

Посмотреть данные сети
docker network inspect <имя сети>

Создать сеть докера
docker network create <имя сети>

Запустить докеры в унисон
docker-compose up -d

Смотреть лог контейнера
docker logs --follow <имя контейнера>